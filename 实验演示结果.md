# Ubuntu系统除零异常响应过程实验演示

## 一、实验环境
- **操作系统**：Ubuntu Linux (Kernel 4.4.0)
- **编译器**：GCC
- **测试程序**：division_zero_test.c, division_zero_with_handler.c

## 二、实验演示

### 实验1：无信号处理器的除零异常

**程序代码**：
```c
c = a / b;  // b = 0时触发异常
```

**运行结果**：
```
输入被除数: 10
输入除数: 0
Floating point exception (core dumped)
进程退出码: 136
```

**分析**：
- 退出码136 = 128 + 8
  - 128：表示进程被信号终止
  - 8：SIGFPE信号编号

### 实验2：带信号处理器的除零异常

**程序代码**：
```c
signal(SIGFPE, sigfpe_handler);  // 注册处理函数
c = a / b;  // b = 0时触发异常
```

**运行结果**：
```
注册SIGFPE信号处理函数...
输入被除数: 10
输入除数: 0
执行除法运算: 10 / 0

===========================================
捕获到信号: SIGFPE (信号编号: 8)
异常类型: 浮点异常 (Floating Point Exception)
原因: 除数为0
===========================================
```

**分析**：
- 程序成功捕获SIGFPE信号
- 执行自定义处理函数
- 优雅地处理异常并提供详细信息

### 实验3：系统调用追踪

使用`strace`追踪系统调用，观察到关键信息：
```
read(0, "10\n0\n", 4096) = 5
--- SIGFPE {si_signo=SIGFPE, si_code=FPE_INTDIV, si_addr=0x561f1a2e21ff} ---
+++ killed by SIGFPE +++
```

**关键信息**：
- `si_signo=SIGFPE`：信号类型
- `si_code=FPE_INTDIV`：整数除法异常
- `si_addr=0x561f1a2e21ff`：触发异常的指令地址

## 三、操作系统异常响应详细过程

### 第一层：硬件层（CPU）

```
┌─────────────────────────────────────┐
│  1. 用户程序执行 c = a / b         │
│     其中 b = 0                      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  2. CPU的ALU执行DIV指令             │
│     检测到除数为0                   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  3. CPU产生除法错误异常             │
│     中断向量号：#0 (Divide Error)   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  4. CPU触发中断                     │
│     准备切换到内核模式              │
└───────────────────────────────────────┘
```

**技术细节**：
- x86架构中，DIV/IDIV指令检测除数为0
- 触发#DE（Divide Error）异常，中断向量0
- CPU自动切换到Ring 0（内核模式）

### 第二层：内核层（Linux Kernel）

```
┌─────────────────────────────────────┐
│  1. CPU切换到内核模式（Ring 0）    │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  2. 保存用户态上下文到内核栈       │
│     - 通用寄存器 (EAX, EBX, ...)   │
│     - 指令指针 (EIP/RIP)           │
│     - 栈指针 (ESP/RSP)             │
│     - 标志寄存器 (EFLAGS)          │
│     - 代码段/数据段选择器 (CS, DS) │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  3. 查找中断描述符表（IDT）        │
│     找到中断向量0的处理函数        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  4. 调用内核异常处理函数           │
│     do_divide_error()              │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  5. 内核识别异常类型               │
│     确定需要发送SIGFPE信号         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  6. 构造信号信息                   │
│     siginfo_t结构：                │
│     - si_signo = SIGFPE            │
│     - si_code = FPE_INTDIV         │
│     - si_addr = 异常指令地址       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  7. 发送信号到进程                 │
│     send_sig_info(SIGFPE, ...)     │
│     信号保存在task_struct中        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  8. 准备返回用户态                 │
│     检查进程是否有待处理信号       │
└───────────────────────────────────────┘
```

**关键内核数据结构**：

1. **IDT（中断描述符表）**：
```c
gate_desc idt_table[256];  // 存储256个中断/异常处理入口
```

2. **task_struct（进程描述符）**：
```c
struct task_struct {
    ...
    struct signal_struct *signal;  // 信号处理信息
    sigset_t blocked, real_blocked;  // 阻塞的信号
    struct sigpending pending;  // 待处理的信号队列
    ...
};
```

3. **siginfo_t（信号信息）**：
```c
siginfo_t {
    int si_signo;   // 信号编号：SIGFPE (8)
    int si_code;    // 信号代码：FPE_INTDIV
    void *si_addr;  // 触发异常的地址
};
```

### 第三层：进程层（User Process）

```
┌─────────────────────────────────────┐
│  1. 内核返回用户态前               │
│     检查待处理信号                 │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  2. 发现SIGFPE信号待处理           │
└──────────────┬──────────────────────┘
               │
               ▼
       ┌───────┴────────┐
       │                │
       ▼                ▼
┌─────────────┐  ┌──────────────────┐
│ 有处理函数  │  │  无处理函数      │
│ (实验2)     │  │  (实验1)         │
└──────┬──────┘  └────────┬─────────┘
       │                  │
       ▼                  ▼
┌─────────────┐  ┌──────────────────┐
│ 执行自定义  │  │  执行默认行为    │
│ 处理函数：  │  │                  │
│             │  │  1. 终止进程     │
│ 1. 打印信息 │  │  2. 生成core文件 │
│ 2. 清理资源 │  │  3. 返回退出码   │
│ 3. 退出程序 │  │     136          │
└─────────────┘  └──────────────────┘
```

**信号处理函数调用栈**：
```
用户程序正常执行
    ↓
触发除零异常
    ↓
内核处理并发送SIGFPE
    ↓
内核准备返回用户态
    ↓
内核设置用户态栈，准备调用信号处理函数
    ↓
返回用户态，执行信号处理函数
    ↓
sigfpe_handler()执行
    ↓
处理完毕，调用exit()或返回
```

## 四、关键技术点深入分析

### 1. 特权级切换（Ring 3 → Ring 0）

```
用户态 (Ring 3)           内核态 (Ring 0)
┌──────────────┐         ┌──────────────┐
│ 用户栈       │         │ 内核栈       │
│              │         │              │
│ [用户数据]   │         │ [保存的寄存器]│
│              │         │ SS           │
│              │         │ ESP          │
│              │         │ EFLAGS       │
└──────────────┘         │ CS           │
                         │ EIP          │
                         │ Error Code   │
                         └──────────────┘
```

**切换过程**：
1. CPU自动保存SS, ESP, EFLAGS, CS, EIP到内核栈
2. 加载内核态的CS, EIP（从IDT获取）
3. 加载内核态的SS, ESP
4. CPU现在运行在Ring 0，可以执行特权指令

### 2. 信号传递机制

```
内核空间                    用户空间
┌─────────────┐           ┌─────────────┐
│ task_struct │           │ 用户进程    │
│             │           │             │
│ pending ────┼──→ SIGFPE │             │
│             │           │             │
└─────────────┘           └─────────────┘
       │                         ▲
       │                         │
       │ 返回用户态时             │
       │ do_signal()             │
       └─────────────────────────┘
              调用信号处理函数
```

**信号处理步骤**：
1. 内核在`task_struct->pending`中标记SIGFPE信号
2. 从内核态返回用户态时，调用`do_signal()`
3. `do_signal()`检查待处理信号
4. 如果有信号处理函数，设置用户栈调用它
5. 如果无处理函数，执行默认动作（终止+core dump）

### 3. Core Dump生成

```bash
# 查看core dump限制
$ ulimit -c
0  # 0表示不生成core文件

# 设置core文件大小限制（单位：KB）
$ ulimit -c unlimited

# 再次运行程序，会生成core文件
$ ./division_zero_test
Floating point exception (core dumped)

# 使用gdb调试core文件
$ gdb ./division_zero_test core
(gdb) bt  # 查看调用栈
(gdb) info registers  # 查看寄存器状态
```

### 4. SIGFPE信号详细信息

| 属性           | 值                    | 说明                          |
|----------------|----------------------|-------------------------------|
| 信号编号       | 8                    | SIGFPE                        |
| 信号名称       | SIGFPE               | Floating Point Exception      |
| 默认动作       | Term + Core          | 终止进程并生成core dump       |
| 可以捕获       | 是                   | 可以使用signal()或sigaction() |
| 可以忽略       | 否（不推荐）         | 忽略会导致未定义行为          |
| 可以阻塞       | 否（不推荐）         | 阻塞会导致未定义行为          |

**FPE_INTDIV（整数除零）子代码**：
```c
// si_code 可能的值
#define FPE_INTDIV  1  // 整数除以零
#define FPE_INTOVF  2  // 整数溢出
#define FPE_FLTDIV  3  // 浮点除以零
#define FPE_FLTOVF  4  // 浮点溢出
#define FPE_FLTUND  5  // 浮点下溢
#define FPE_FLTRES  6  // 浮点结果不精确
#define FPE_FLTINV  7  // 浮点非法操作
```

## 五、完整流程时序图

```
时间线
  │
  │  用户态                    内核态
  │
  ├── [执行 c = a/b]
  │         ↓
  ├── [CPU检测除零] ─────────→ [触发中断]
  │                              ↓
  │                         [切换到Ring 0]
  │                              ↓
  │                         [保存上下文]
  │                              ↓
  │                         [查找IDT]
  │                              ↓
  │                         [调用do_divide_error()]
  │                              ↓
  │                         [构造SIGFPE信号]
  │                              ↓
  │                         [发送信号到进程]
  │                              ↓
  │                         [准备返回用户态]
  │                              ↓
  │                         [检查待处理信号]
  │                              ↓
  ├── [返回用户态] ←──────── [设置信号处理栈]
  │         ↓
  ├── [执行sigfpe_handler()]
  │         ↓
  ├── [打印异常信息]
  │         ↓
  ├── [调用exit(1)]
  │         ↓
  ├── [进程终止]
  ▼
```

## 六、相关系统调用和内核函数

### 用户空间API
```c
// 注册信号处理函数（简单版）
sighandler_t signal(int signum, sighandler_t handler);

// 注册信号处理函数（完整版）
int sigaction(int signum, const struct sigaction *act,
              struct sigaction *oldact);

// 发送信号给进程
int kill(pid_t pid, int sig);
```

### 内核函数调用链
```
do_divide_error()          // arch/x86/kernel/traps.c
  └─> do_error_trap()
      └─> do_trap()
          └─> force_sig_fault()
              └─> send_signal()
                  └─> __send_signal()
                      └─> complete_signal()
```

## 七、实验总结

### 关键发现

1. **硬件层面**：CPU的除法单元检测到除零错误，立即产生中断
2. **内核层面**：Linux内核捕获硬件中断，转换为SIGFPE信号
3. **进程层面**：进程接收信号，执行处理函数或默认行为

### 操作系统核心机制

1. **中断机制**：CPU与内核的通信桥梁
2. **特权级切换**：保护内核安全，隔离用户程序
3. **信号机制**：Unix/Linux进程间通信和异常处理的重要手段
4. **上下文保存/恢复**：确保异常处理后能正确返回

### 退出码分析

```
退出码 136 的含义：
136 = 128 + 8
      │     │
      │     └─→ SIGFPE信号编号 (8)
      └───────→ 被信号终止标志 (128)
```

这种编码方式让shell和父进程能够区分：
- 正常退出（0-127）
- 被信号终止（128-255）
- 具体是哪个信号（退出码 - 128）

## 八、参考资料

### Linux内核源码
- `arch/x86/kernel/traps.c` - x86异常处理
- `kernel/signal.c` - 信号处理机制
- `arch/x86/include/asm/idtentry.h` - 中断描述符表定义

### Intel手册
- Intel® 64 and IA-32 Architectures Software Developer's Manual
- Volume 3: System Programming Guide
- Chapter 6: Interrupt and Exception Handling

### POSIX标准
- IEEE Std 1003.1-2017 (POSIX.1-2017)
- Section: Signal Concepts
