# Ubuntu系统除零异常响应过程：用户栈和内核栈变化详解

## 一、栈的基本概念

### 1. 用户栈（User Stack）
- **位置**：用户空间（虚拟地址0x00000000 - 0xBFFFFFFF，x86）
- **用途**：存储用户程序的函数调用、局部变量、返回地址
- **特权级**：Ring 3
- **栈指针**：ESP/RSP（用户态）

### 2. 内核栈（Kernel Stack）
- **位置**：内核空间（虚拟地址0xC0000000 - 0xFFFFFFFF，x86）
- **用途**：存储内核函数调用、保存用户态上下文
- **特权级**：Ring 0
- **栈指针**：ESP/RSP（内核态）
- **大小**：通常8KB（两个页面）

## 二、除零异常前的栈状态

### 用户栈（异常发生前）

```
用户栈 (向下增长)              虚拟地址
┌────────────────────────┐
│  命令行参数/环境变量    │    0xBFFFFFFF (栈顶)
├────────────────────────┤
│  main()的栈帧           │
│  ┌──────────────────┐  │
│  │ 局部变量 c       │  │
│  │ 局部变量 b = 0   │  │
│  │ 局部变量 a = 10  │  │
│  │ 保存的EBP        │  │
│  │ 返回地址         │  │    ← 当前ESP指向这里
│  └──────────────────┘  │
├────────────────────────┤
│  _start()的栈帧        │
└────────────────────────┘
         ↓ (栈增长方向)
```

**寄存器状态**：
```
EIP  = 0x08048XXX  (执行 c=a/b 的指令地址)
ESP  = 0xBFFFE7C0  (用户栈顶指针)
EBP  = 0xBFFFE7D0  (当前栈帧基址)
CS   = 0x0023      (用户代码段选择子, RPL=3)
SS   = 0x002B      (用户栈段选择子, RPL=3)
EFLAGS = 0x00000246
```

### 内核栈（异常发生前）
此时内核栈为空，因为进程运行在用户态。

## 三、除零异常发生时的栈变化

### 第1步：CPU检测到除零异常

```
执行指令: DIV EBX  (或 IDIV EBX)
├─ EAX = 10 (被除数)
└─ EBX = 0  (除数) ← CPU检测到异常！
```

CPU硬件自动执行以下操作：

### 第2步：CPU自动切换栈和特权级

```
切换前 (Ring 3)           切换后 (Ring 0)
┌───────────────┐       ┌───────────────┐
│ CS  = 0x0023  │  →    │ CS  = 0x0010  │
│ EIP = 0x8048XXX│  →   │ EIP = handler │
│ SS  = 0x002B  │  →    │ SS  = 0x0018  │
│ ESP = 用户栈   │  →    │ ESP = 内核栈  │
└───────────────┘       └───────────────┘
```

### 第3步：CPU自动保存用户态上下文到内核栈

**内核栈变化（CPU硬件自动完成）**：

```
内核栈 (向下增长)                    地址偏移
┌─────────────────────────────┐
│                             │
│  (内核栈顶，未使用部分)      │    +8KB
├─────────────────────────────┤
│  ← CPU自动压入以下内容 ←     │
├─────────────────────────────┤
│  [5] 用户SS                 │    ESP+16  (0x002B)
├─────────────────────────────┤
│  [4] 用户ESP                │    ESP+12  (0xBFFFE7C0)
├─────────────────────────────┤
│  [3] 用户EFLAGS             │    ESP+8   (0x00000246)
├─────────────────────────────┤
│  [2] 用户CS                 │    ESP+4   (0x0023)
├─────────────────────────────┤
│  [1] 用户EIP                │    ESP+0   (0x08048XXX) ← 当前ESP
├─────────────────────────────┤
│  [0] 错误代码 (某些异常)    │    (除零异常无错误代码)
└─────────────────────────────┘
         ↓ (栈增长方向)
```

**关键点**：
- CPU在切换到内核态时，自动切换到**内核栈**
- 内核栈地址从进程的 `task_struct->stack` 获取
- CPU自动保存5个关键寄存器（SS, ESP, EFLAGS, CS, EIP）

### 第4步：内核异常处理函数继续保存寄存器

内核的 `do_divide_error()` 入口代码继续保存其他寄存器：

```
内核栈 (继续压入)
├─────────────────────────────┤
│  [CPU自动保存的5个寄存器]   │    (上面已保存)
├─────────────────────────────┤
│  ← 内核代码手动压入 ←       │
├─────────────────────────────┤
│  DS  (数据段)               │
├─────────────────────────────┤
│  ES  (扩展段)               │
├─────────────────────────────┤
│  FS  (FS段)                 │
├─────────────────────────────┤
│  GS  (GS段)                 │
├─────────────────────────────┤
│  EAX (10)                   │
├─────────────────────────────┤
│  EBX (0)                    │
├─────────────────────────────┤
│  ECX                        │
├─────────────────────────────┤
│  EDX                        │
├─────────────────────────────┤
│  ESI                        │
├─────────────────────────────┤
│  EDI                        │
├─────────────────────────────┤
│  EBP (0xBFFFE7D0)           │    ← 当前ESP
├─────────────────────────────┤
│  pt_regs 结构结束           │
└─────────────────────────────┘
```

这些寄存器被保存在 `struct pt_regs` 结构中：

```c
struct pt_regs {
    unsigned long bx;
    unsigned long cx;
    unsigned long dx;
    unsigned long si;
    unsigned long di;
    unsigned long bp;
    unsigned long ax;
    unsigned long ds;
    unsigned long es;
    unsigned long fs;
    unsigned long gs;
    unsigned long orig_ax;
    unsigned long ip;      // 用户EIP
    unsigned long cs;      // 用户CS
    unsigned long flags;   // 用户EFLAGS
    unsigned long sp;      // 用户ESP
    unsigned long ss;      // 用户SS
};
```

## 四、信号处理过程中的栈变化

### 第5步：内核处理异常并准备信号

内核栈继续增长，调用内核函数：

```
内核栈 (函数调用栈)
├─────────────────────────────┤
│  pt_regs结构 (保存的寄存器)  │
├─────────────────────────────┤
│  do_divide_error() 栈帧     │
│  ├─ 局部变量               │
│  └─ 参数                   │
├─────────────────────────────┤
│  do_error_trap() 栈帧       │
├─────────────────────────────┤
│  do_trap() 栈帧             │
├─────────────────────────────┤
│  force_sig_fault() 栈帧     │
│  ├─ siginfo_t 结构         │
│  │  ├─ si_signo = 8       │
│  │  ├─ si_code = FPE_INTDIV│
│  │  └─ si_addr = 0x8048XXX│
│  └─ ...                    │
├─────────────────────────────┤
│  send_signal() 栈帧         │    ← 当前ESP
└─────────────────────────────┘
```

### 第6步：返回用户态前设置信号处理栈帧

内核在返回用户态前，修改用户栈，设置信号处理函数的调用环境：

**用户栈变化（内核修改）**：

```
用户栈 (内核在返回前修改)
┌─────────────────────────────┐
│  main()的原始栈帧           │    (保留)
├─────────────────────────────┤
│  ← 内核构造的信号栈帧 ←     │
├─────────────────────────────┤
│  [信号处理返回地址]          │    → 指向sigreturn系统调用
├─────────────────────────────┤
│  信号编号参数 (8)           │    sigfpe_handler的参数
├─────────────────────────────┤
│  保存的用户上下文            │
│  (ucontext_t结构)           │
│  ┌───────────────────────┐  │
│  │ uc_mcontext           │  │
│  │  ├─ EIP = 0x8048XXX   │  │    (除零指令地址)
│  │  ├─ ESP = 0xBFFFE7C0  │  │
│  │  ├─ EBP = 0xBFFFE7D0  │  │
│  │  ├─ EAX = 10         │  │
│  │  ├─ EBX = 0          │  │
│  │  └─ ...              │  │
│  ├─ uc_sigmask          │  │
│  └─ uc_stack            │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  siginfo_t结构              │
│  ├─ si_signo = 8           │
│  ├─ si_code = FPE_INTDIV   │
│  └─ si_addr = 0x8048XXX    │
└─────────────────────────────┘    ← 新的ESP
         ↓
```

**内核准备返回时的寄存器设置**：
```
EIP  = sigfpe_handler  (信号处理函数地址)
ESP  = 修改后的用户栈指针
EBP  = (保持或修改)
CS   = 0x0023  (恢复用户代码段)
SS   = 0x002B  (恢复用户栈段)
```

### 第7步：CPU恢复用户态并执行信号处理函数

内核执行 `IRET` 指令返回用户态：

```
内核栈 (弹出保存的上下文)           用户态执行
┌─────────────────────────┐      ┌──────────────────────┐
│ POP 用户EIP (handler)   │  →   │ EIP = sigfpe_handler │
│ POP 用户CS              │  →   │ CS  = 0x0023         │
│ POP 用户EFLAGS          │  →   │ EFLAGS恢复           │
│ POP 用户ESP             │  →   │ ESP = 用户栈         │
│ POP 用户SS              │  →   │ SS  = 0x002B         │
└─────────────────────────┘      └──────────────────────┘
                                          ↓
                                  执行信号处理函数
```

**用户栈（执行信号处理函数时）**：

```
用户栈 (信号处理函数执行)
├─────────────────────────────┤
│  sigfpe_handler() 栈帧      │
│  ├─ 局部变量               │
│  ├─ 保存的EBP              │    ← 当前EBP
│  └─ 返回地址(sigreturn)    │    ← 当前ESP
├─────────────────────────────┤
│  内核构造的信号栈帧         │    (如上所示)
├─────────────────────────────┤
│  main()的原始栈帧           │
└─────────────────────────────┘
```

## 五、信号处理完成后的栈恢复

### 方式1：信号处理函数调用exit()

```c
void sigfpe_handler(int signum) {
    printf("捕获到SIGFPE信号\n");
    exit(1);  // 不返回
}
```

**栈状态**：进程终止，所有栈都被销毁。

### 方式2：信号处理函数返回

```c
void sigfpe_handler(int signum) {
    printf("捕获到SIGFPE信号\n");
    return;  // 返回
}
```

**返回过程**：
1. 信号处理函数执行 `RET` 指令
2. 返回到 `sigreturn` 系统调用（内核设置的返回地址）
3. `sigreturn` 从用户栈恢复保存的上下文
4. 恢复到异常发生前的状态（**注意：会再次触发除零异常！**）

**用户栈恢复**：

```
用户栈 (恢复后)
┌────────────────────────────┐
│  main()的栈帧               │
│  ┌──────────────────┐      │
│  │ 局部变量 c       │      │
│  │ 局部变量 b = 0   │      │
│  │ 局部变量 a = 10  │      │
│  │ 保存的EBP        │      │    ← EBP恢复
│  │ 返回地址         │      │    ← ESP恢复
│  └──────────────────┘      │
└────────────────────────────┘

EIP恢复到 c = a/b; 的地址 → 再次触发异常！
```

### 方式3：使用 sigsetjmp/siglongjmp（不死循环）

```c
sigjmp_buf jmpbuf;

void sigfpe_handler(int signum) {
    printf("捕获到SIGFPE信号\n");
    siglongjmp(jmpbuf, 1);  // 跳转到sigsetjmp位置
}

int main() {
    signal(SIGFPE, sigfpe_handler);

    if (sigsetjmp(jmpbuf, 1) == 0) {
        // 第一次执行，保存环境
        c = a / b;  // 触发异常
    } else {
        // siglongjmp跳转到这里
        printf("异常已处理，继续执行\n");
    }
}
```

**栈恢复过程**：

```
1. sigsetjmp(jmpbuf, 1) 保存环境：
   ┌─────────────────────┐
   │ jmpbuf结构          │
   │ ├─ 保存的EBP       │
   │ ├─ 保存的ESP       │
   │ ├─ 保存的EIP       │
   │ └─ 信号掩码        │
   └─────────────────────┘

2. 发生异常，执行sigfpe_handler

3. siglongjmp(jmpbuf, 1) 恢复环境：
   - 从jmpbuf恢复ESP, EBP, EIP
   - 恢复信号掩码
   - 设置返回值为1
   - 跳转到sigsetjmp位置

4. 用户栈恢复到sigsetjmp时的状态：
   ┌────────────────────────────┐
   │  main()的栈帧               │
   │  ├─ jmpbuf                 │
   │  ├─ 局部变量               │
   │  ├─ 保存的EBP              │    ← 恢复到这里
   │  └─ 返回地址               │    ← 恢复到这里
   └────────────────────────────┘

   EIP指向sigsetjmp后的代码 (else分支)
```

## 六、完整的栈切换流程图

```
时间线    用户栈                        内核栈
  │
  │  ┌──────────────┐              (空)
  │  │ main栈帧     │
  │  │ a=10, b=0    │
  │  └──────────────┘
  │         ↓
  ├─ [执行 c=a/b] ─────────→ [CPU检测异常]
  │                                  ↓
  │                         ┌─────────────────┐
  │                         │ CPU自动保存：   │
  │                         │ - 用户SS        │
  │                         │ - 用户ESP       │
  │                         │ - 用户EFLAGS    │
  │                         │ - 用户CS        │
  │                         │ - 用户EIP       │
  │                         └─────────────────┘
  │                                  ↓
  │                         ┌─────────────────┐
  │                         │ 内核手动保存：   │
  │                         │ - 段寄存器      │
  │                         │ - 通用寄存器     │
  │                         │ (pt_regs结构)   │
  │                         └─────────────────┘
  │                                  ↓
  │                         ┌─────────────────┐
  │                         │ 内核函数调用栈： │
  │                         │ - do_divide_error│
  │                         │ - do_trap       │
  │                         │ - send_signal   │
  │                         └─────────────────┘
  │                                  ↓
  │  ┌──────────────┐      [内核修改用户栈]
  │  │ 内核构造：   │              ↓
  │  │ - ucontext   │      ┌─────────────────┐
  │  │ - siginfo    │      │ 弹出内核栈      │
  │  │ - 返回地址   │      │ 恢复寄存器      │
  │  └──────────────┘      └─────────────────┘
  │  │ main栈帧     │              ↓
  │  └──────────────┘      [执行IRET返回用户态]
  │         ↓                      ↓
  ├─ [执行sigfpe_handler] ←──────┘
  │  ┌──────────────┐              (空)
  │  │ handler栈帧  │
  │  ├──────────────┤
  │  │ 信号栈帧     │
  │  ├──────────────┤
  │  │ main栈帧     │
  │  └──────────────┘
  │         ↓
  ├─ [调用siglongjmp]
  │  ┌──────────────┐
  │  │ main栈帧     │    (恢复到sigsetjmp时的状态)
  │  └──────────────┘
  │         ↓
  ├─ [继续执行else分支]
  ▼
```

## 七、关键数据结构

### 1. 进程描述符中的栈信息

```c
struct task_struct {
    ...
    void *stack;                    // 内核栈指针
    struct thread_struct thread;    // 线程信息
    ...
};

struct thread_struct {
    unsigned long sp0;              // 内核栈顶指针
    unsigned long sp;               // 当前栈指针
    ...
};
```

### 2. TSS（任务状态段）

```c
struct tss_struct {
    unsigned long sp0;              // Ring 0 栈指针
    unsigned long ss0;              // Ring 0 栈段
    ...
};
```

CPU在特权级切换时，从TSS中获取内核栈地址。

### 3. pt_regs（寄存器保存结构）

```c
struct pt_regs {
    // 内核手动保存
    unsigned long bx, cx, dx, si, di, bp, ax;
    unsigned long ds, es, fs, gs;
    unsigned long orig_ax;

    // CPU自动保存
    unsigned long ip;               // 用户EIP
    unsigned long cs;               // 用户CS
    unsigned long flags;            // 用户EFLAGS
    unsigned long sp;               // 用户ESP
    unsigned long ss;               // 用户SS
};
```

### 4. ucontext（用户上下文）

```c
typedef struct ucontext {
    unsigned long uc_flags;
    struct ucontext *uc_link;
    stack_t uc_stack;               // 信号栈信息
    struct sigcontext uc_mcontext;  // 保存的寄存器
    sigset_t uc_sigmask;            // 信号掩码
} ucontext_t;
```

### 5. siginfo（信号信息）

```c
typedef struct siginfo {
    int si_signo;                   // 信号编号: 8 (SIGFPE)
    int si_errno;                   // 错误码
    int si_code;                    // 信号代码: FPE_INTDIV

    union {
        ...
        struct {
            void *si_addr;          // 触发异常的指令地址
        } _sigfault;
        ...
    } _sifields;
} siginfo_t;
```

## 八、内存布局总览

```
虚拟地址空间 (32位 x86)
┌─────────────────────────────┐  0xFFFFFFFF
│      内核空间                │
│  ┌────────────────────────┐ │  0xFFFFE000
│  │  当前进程的内核栈      │ │  (8KB)
│  │  ┌──────────────────┐  │ │
│  │  │ 保存的用户寄存器 │  │ │
│  │  │ (pt_regs)        │  │ │
│  │  ├──────────────────┤  │ │
│  │  │ 内核函数调用栈   │  │ │
│  │  └──────────────────┘  │ │
│  │         ↓              │ │  (向下增长)
│  └────────────────────────┘ │
│                             │
│      内核代码和数据          │
├─────────────────────────────┤  0xC0000000
│      用户空间                │
│                             │
│  ┌────────────────────────┐ │  0xBFFFFFFF
│  │  用户栈                │ │
│  │  ┌──────────────────┐  │ │
│  │  │ 信号处理栈帧     │  │ │
│  │  ├──────────────────┤  │ │
│  │  │ main()栈帧       │  │ │
│  │  └──────────────────┘  │ │
│  │         ↓              │ │  (向下增长)
│  └────────────────────────┘ │
│                             │
│      堆 (Heap)              │
│         ↑                   │  (向上增长)
│                             │
│      数据段 (.data, .bss)   │
│      代码段 (.text)         │
└─────────────────────────────┘  0x08048000

注：用户空间和内核空间通过页表隔离，
    用户态无法直接访问内核空间。
```

## 九、实验验证：使用GDB观察栈变化

### 1. 查看用户栈

```bash
(gdb) break main
(gdb) run
(gdb) info frame          # 查看当前栈帧
(gdb) info registers ebp esp eip
(gdb) x/20x $esp          # 查看栈内容
```

### 2. 查看信号处理时的栈

```bash
(gdb) break sigfpe_handler
(gdb) continue
(gdb) backtrace           # 查看调用栈
(gdb) info frame          # 查看栈帧信息
(gdb) x/50x $esp          # 查看栈内容
```

### 3. 查看内核栈（需要内核调试）

```bash
# 使用crash工具或者内核调试器
crash> bt                 # 查看内核调用栈
crash> task_struct        # 查看进程描述符
crash> dis do_divide_error # 反汇编异常处理函数
```

## 十、总结

### 关键点

1. **双栈机制**：用户栈和内核栈分离，保证内核安全
2. **自动切换**：CPU在特权级切换时自动切换栈（从TSS获取内核栈地址）
3. **上下文保存**：
   - CPU自动保存：SS, ESP, EFLAGS, CS, EIP
   - 内核手动保存：段寄存器、通用寄存器
4. **信号处理栈**：内核在用户栈上构造信号处理环境
5. **栈恢复**：通过sigreturn系统调用恢复用户态上下文

### 栈变化时间线

```
1. 异常前：     用户栈(main)  |  内核栈(空)
2. CPU切换：    用户栈(main)  |  内核栈(保存的寄存器)
3. 内核处理：   用户栈(main)  |  内核栈(函数调用链)
4. 准备返回：   用户栈(main+信号栈帧) | 内核栈(弹出)
5. 信号处理：   用户栈(handler+信号栈帧+main) | 内核栈(空)
6. 恢复/跳转：  用户栈(main)  |  内核栈(空)
```

这个机制确保了：
- **隔离性**：用户程序无法访问内核栈
- **安全性**：特权级切换由硬件保证
- **可恢复性**：保存完整上下文，可以恢复到任意状态
